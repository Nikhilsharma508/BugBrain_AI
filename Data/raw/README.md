# `data/raw/` — Immutable Source Files

Copies of source data files used by the pipeline. These files should not be modified after being placed here.

The original source CSV lives in `Data/bug_report.csv` (the existing project folder). Use `scripts/load_csv_data.py` to copy and prepare it for pipeline use.
