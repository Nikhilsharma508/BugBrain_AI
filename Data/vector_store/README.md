# `data/vector_store/` — FAISS / ChromaDB Index Files

Stores the vector database index files used for duplicate detection. These are binary files generated by `scripts/build_vector_index.py` and updated as new reports are processed.

**Do not commit these to Git** — they are large binary files that should be regenerated from source data.
