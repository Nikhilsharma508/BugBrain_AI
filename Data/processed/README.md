# `data/processed/` — Cleaned & Transformed Outputs

Stores the output of preprocessing steps: noise-filtered reports, extracted summaries, and structured JSON results. These files are generated by the pipeline and should not be committed to Git.
