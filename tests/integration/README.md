# `tests/integration/` — Integration Tests

End-to-end tests that verify multiple modules working together. These tests may require API keys and a running vector store. Run with `pytest tests/integration/ -v`.
